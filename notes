A socket name in the Internet domain is an Internet address, made up of a 32-bit IP address and a 16-bit port address.

FIONBIO to set sockets I/O as non-blocking

https://ngircd.barton.de/ serveur irc pour check le comportement avec le notre

" 8.2
To provide useful 'non-buffered' network IO for clients and servers,
   each connection is given its own private 'input buffer' in which the
   results of the most recent read and parsing are kept.  A buffer size
   of 512 bytes is used so as to hold 1 full message, although, this
   will usually hold several commands.  The private buffer is parsed
   after every read operation for valid messages.  When dealing with
   multiple messages from one client in the buffer, care should be taken
   in case one happens to cause the client to be 'removed'. "

Sylvain :
faire cast c++ dans bind(...)
comprendre comment fonctionne IRC de maniere generale
vois avec Milos que faire des donnees crees par l serveur sur le screen de Ben
comprendre comment initier la connexion entre serveur et client en utilisant la norme RFC1459


Benjamin :

Ca ne sert a rien de faire des objets puisqu'on ne fait de bibliotheque


tous les deux :
creer des classes ?
implementer un serveur d'identite https://en.wikipedia.org/wiki/Ident_protocol
                                  https://datatracker.ietf.org/doc/html/rfc1413
ajouter -Wall -Wextra -Werror